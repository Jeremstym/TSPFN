defaults:
  - default

_target_: data.data_module.EOSDataModule
data_roots: ${oc.env:EOS_DATA_PATH}
time_series_length: 128
num_channels: 3
num_classes: 2

predict_losses:
  classification:
    _target_: torch.nn.CrossEntropyLoss